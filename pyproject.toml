[project]
name = "wandering-wings"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "duckdb>=1.5.5",
    "jupyter>=1.1.1",
    "numpy>=2.5.3",
    "pandas>=3.0.6",
    "polars>=1.44.2",
    "pyarrow>=25.0.1",
]

[dependency-groups]
dev = [
    "ruff>=0.16.9",
]
